# 📸 Настройка локальных изображений

## Текущий статус

✅ Код обновлен для использования локальных изображений  
⏳ Необходимо добавить файлы изображений в проект

## Инструкция по добавлению изображений

### Шаг 1: Создайте структуру папок

```bash
mkdir -p public/images/{hero,events,muse-tv,about}
```

### Шаг 2: Скачайте изображения

Вам нужно скачать следующие файлы с CDN и разместить их по указанным путям:

#### Для страницы Events (`/events`)

```bash
# Mobile background
curl "https://cdn.poehali.dev/files/16b2656a-d7ad-4d09-996c-22fdd08827b8.jpg" \
  -o public/images/events/mobile-bg.jpg

# Left image  
curl "https://cdn.poehali.dev/files/f730e14d-c58a-4ac4-9b0a-d2d7c2378616.jpg" \
  -o public/images/events/left.jpg

# Right image
curl "https://cdn.poehali.dev/files/4a9523f3-5fc0-400f-b4c7-6dd69bdbd217.jpg" \
  -o public/images/events/right.jpg
```

#### Для страницы MUSE TV (`/muse-tv`)

```bash
# Background
curl "https://cdn.poehali.dev/files/0a4d076c-a60c-4a0a-9bf1-eab254a3f261.png" \
  -o public/images/muse-tv/background.png
```

#### Для главной страницы - About секция (`/`)

```bash
# Founder image
curl "https://cdn.poehali.dev/files/f8cbb77a-0ff7-4aa5-b210-4095cac6db26.jpg" \
  -o public/images/about/founder.jpg
```

#### Для Hero секции (главная страница)

Hero изображения загружаются из API динамически. После того как API вернет URL:
1. Скачайте каждое изображение
2. Разместите в `public/images/hero/` с названиями `left.jpg`, `center.jpg`, `right.jpg`
3. Обновите маппинг в `src/utils/imageLoader.ts`

### Шаг 3: Проверьте результат

После добавления изображений:
1. Перезапустите dev server (если нужно)
2. Откройте сайт в браузере
3. Проверьте Network вкладку - все изображения должны загружаться локально

## Преимущества локальных изображений

✅ **Мгновенная загрузка** - нет задержки на запрос к CDN  
✅ **Работа офлайн** - сайт работает без интернета  
✅ **Оптимизация** - можно создать WebP варианты и responsive версии  
✅ **Кэширование** - браузер эффективнее кэширует локальные файлы  
✅ **Отладка** - проще тестировать и отлаживать

## Дополнительная оптимизация (опционально)

### Создание WebP версий

```bash
# Установите cwebp
brew install webp  # macOS
# или apt-get install webp  # Linux

# Конвертируйте JPG в WebP
for img in public/images/**/*.jpg; do
  cwebp -q 85 "$img" -o "${img%.jpg}.webp"
done
```

### Создание responsive версий

```bash
# Установите ImageMagick
brew install imagemagick

# Создайте @2x версии
for img in public/images/**/*.jpg; do
  convert "$img" -resize 50% "${img%.jpg}@2x.jpg"
done
```

Затем обновите `src/utils/imageLoader.ts` для использования srcSet.

## Структура файлов после настройки

```
public/
└── images/
    ├── hero/
    │   ├── left.jpg
    │   ├── center.jpg
    │   └── right.jpg
    ├── events/
    │   ├── mobile-bg.jpg
    │   ├── left.jpg
    │   └── right.jpg
    ├── muse-tv/
    │   └── background.png
    └── about/
        └── founder.jpg
```

## Устранение проблем

**Изображения не загружаются?**
1. Проверьте пути в `public/images/`
2. Убедитесь что файлы названы правильно
3. Проверьте Network вкладку в DevTools
4. Попробуйте hard refresh (Cmd+Shift+R)

**Hero изображения не показываются?**
1. Проверьте что API возвращает URL
2. Добавьте mapping в `src/utils/imageLoader.ts`:
   ```typescript
   const IMAGE_MAP: Record<string, string> = {
     // ... existing mappings
     'YOUR_IMAGE_ID': '/images/hero/left.jpg',
   };
   ```

**Нужна помощь?**
Откройте консоль браузера (F12) и посмотрите на ошибки загрузки изображений.
