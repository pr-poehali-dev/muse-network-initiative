# 📸 Руководство по загрузке изображений через админку

## ✅ Что реализовано

Теперь в админке есть раздел "Изображения", который позволяет:
- ✅ Загружать изображения напрямую в проект
- ✅ Автоматически генерировать уникальные имена файлов
- ✅ Просматривать превью перед сохранением
- ✅ Получать готовые URL для использования в коде

---

## 🚀 Как использовать

### Шаг 1: Открыть админку

1. Перейдите на `/admin`
2. Войдите с паролем
3. Нажмите на вкладку **"Изображения"**

### Шаг 2: Загрузить изображение

1. Выберите категорию (Hero, Событие, Эксперт, Другое)
2. **Вариант А:** Перетащите файл в зону загрузки
3. **Вариант Б:** Нажмите на зону и выберите файл

### Шаг 3: Сохранить файл

1. После выбора файла он автоматически **скачается в браузере**
2. Найдите скачанный файл (обычно в папке `Загрузки`)
3. **Переместите** его в `public/images/uploads/` в вашем проекте

### Шаг 4: Использовать URL

URL изображения будет показан в уведомлении:
```
/images/uploads/1704123456789-abc123.jpg
```

Используйте его в коде:
```tsx
<img src="/images/uploads/1704123456789-abc123.jpg" alt="Description" />
```

---

## 📁 Структура файлов

```
public/
└── images/
    └── uploads/           # Папка для загруженных изображений
        ├── 1704123456789-abc123.jpg  # Ваше изображение 1
        ├── 1704123789456-def456.png  # Ваше изображение 2
        └── ...
```

---

## 🎨 Примеры использования

### Hero изображение

```tsx
// В HomepageSection или где настраиваете Hero
heroContent.image_left = '/images/uploads/1704123456-abc.jpg';
```

### Фото эксперта

```tsx
// В форме добавления эксперта
speakerFormData.image = '/images/uploads/1704123789-expert.jpg';
```

### Фото события

```tsx
// В форме события
formData.speakers[0].image = '/images/uploads/1704124000-event.jpg';
```

---

## ⚙️ Настройки

### Ограничения

- **Максимальный размер:** 10 MB
- **Форматы:** JPEG, JPG, PNG, WebP, GIF
- **Автоматическое именование:** timestamp + random string

### Изменить ограничения

В `src/components/admin/ImageUploader.tsx`:

```tsx
<ImageUploader
  maxSizeMB={5}  // Изменить лимит на 5 MB
  accept="image/jpeg,image/png"  // Только JPEG и PNG
/>
```

---

## 💡 Советы

### 1. Оптимизируйте изображения перед загрузкой

```bash
# Используйте tinypng.com или squoosh.app
# Рекомендуемые параметры:
# - JPEG: качество 85%
# - PNG: сжатие 80%
# - WebP: качество 80%
```

### 2. Используйте правильные размеры

| Тип изображения | Ширина | Высота | Формат |
|-----------------|--------|--------|--------|
| Hero (главная) | 1920px | 1080px | JPEG/WebP |
| Фото эксперта | 600px | 800px | JPEG |
| Фото события | 1200px | 800px | JPEG/WebP |
| Миниатюра | 400px | 300px | JPEG |

### 3. Именование файлов

Система автоматически создаст имя вида:
```
timestamp-random.ext
1704123456789-abc123.jpg
```

Это гарантирует:
- ✅ Уникальность (timestamp)
- ✅ Отсутствие конфликтов (random)
- ✅ Правильный формат (ext из оригинала)

### 4. Организация файлов

Хотя все файлы попадают в `uploads/`, вы можете создать подпапки:

```
public/images/uploads/
├── heroes/      # Hero изображения
├── experts/     # Фото экспертов
├── events/      # Фото событий
└── misc/        # Прочее
```

Тогда URL будет:
```
/images/uploads/heroes/hero-main.jpg
```

---

## 🔍 Проверка

### Убедитесь что изображение загружено

1. Откройте `public/images/uploads/` в проводнике
2. Найдите файл по имени из уведомления
3. Проверьте размер файла (не должен быть 0 байт)

### Тест в браузере

Откройте URL напрямую в браузере:
```
http://localhost:5173/images/uploads/YOUR_FILE.jpg
```

Если изображение отображается - всё работает! ✅

---

## 🐛 Устранение проблем

### Изображение не отображается

**Проблема:** URL правильный, но изображение не показывается

**Решение:**
1. Проверьте что файл в `public/images/uploads/`
2. Перезапустите dev server (`bun run dev`)
3. Очистите кэш браузера (Ctrl+Shift+R)

### Файл не скачивается автоматически

**Проблема:** После выбора файла ничего не происходит

**Решение:**
1. Проверьте настройки браузера (разрешите автозагрузку)
2. Проверьте консоль (F12) на ошибки
3. Попробуйте другой браузер

### Превью не показывается

**Проблема:** После загрузки нет превью изображения

**Решение:**
1. Проверьте формат файла (должен быть JPEG/PNG/WebP/GIF)
2. Проверьте размер файла (не более 10 MB)
3. Убедитесь что файл не поврежден

---

## 🎯 Workflow (рекомендуемый)

1. **Подготовка:**
   - Оптимизируйте изображение (tinypng.com)
   - Измените размер если нужно (squoosh.app)

2. **Загрузка:**
   - Откройте админку → "Изображения"
   - Загрузите файл
   - Дождитесь скачивания

3. **Сохранение:**
   - Переместите из Загрузок в `public/images/uploads/`
   - Скопируйте URL из уведомления

4. **Использование:**
   - Вставьте URL в нужное поле админки
   - Или используйте напрямую в коде

5. **Проверка:**
   - Откройте страницу с изображением
   - Убедитесь что всё отображается корректно

---

## 📝 Пример полного цикла

```typescript
// 1. Загружаем через админку
// Получаем: /images/uploads/1704123456-photo.jpg

// 2. Используем в Hero секции
const heroContent = {
  image_left: '/images/uploads/1704123456-photo.jpg',
  image_center: '/images/uploads/1704123789-center.jpg',
  image_right: '/images/uploads/1704124000-right.jpg',
};

// 3. Или в эксперте
const speaker = {
  name: 'Иван Иванов',
  role: 'Эксперт',
  image: '/images/uploads/1704124200-ivan.jpg',
};

// 4. Проверяем на странице
// http://localhost:5173/ - должно показать Hero изображения
```

---

## ✨ Готово!

Теперь вы можете загружать изображения прямо из админки без использования внешних сервисов! 🚀

**Следующий шаг:** Попробуйте загрузить тестовое изображение и проверьте работу.
